os:
  - linux

language: python
python:
  - "3.8-dev"
  - "3.7-dev"
  - "3.6"

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -e .

# command to run tests
script:
  - pytest --cov=./

after_success:
  - codecov